# LiveKit Configuration for Remote Audio Access
# Replace these with your actual LiveKit Cloud project credentials
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=your-api-key
LIVEKIT_API_SECRET=your-api-secret

# Optional: For connecting to specific rooms
LIVEKIT_ROOM_NAME=voice-mcp-room

# Speech-to-Text Configuration
AGENT_STT_BACKEND=whisper  # "whisper" for self-hosted, "openai" for cloud STT
WHISPER_MODEL=base         # base, small, medium, large (larger = better accuracy, slower)

# ElevenLabs TTS (keep existing)
ELEVENLABS_API_KEY=your_elevenlabs_key_here

# LLM Configuration
AGENT_LLM_BACKEND=openai
AGENT_LLM_MODEL=gpt-4o-mini
OPENAI_API_KEY=your_openai_key_here

# Alternative: Local Ollama LLM (completely self-hosted)
# AGENT_LLM_BACKEND=ollama
# OLLAMA_BASE_URL=http://localhost:11434/v1
# AGENT_LLM_MODEL=llama3:8b